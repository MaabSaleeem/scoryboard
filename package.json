{
  "name": "scoryboard-help-centre",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Screenshot capture and Intercom publishing for the Scoryboard help centre",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "capture": "playwright test",
    "capture:collection": "playwright test specs/$COLLECTION",
    "lint:specs": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "typescript": "^5.7.0",
    "@types/node": "^22.10.0"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "yaml": "^2.6.1",
    "sharp": "^0.33.5"
  }
}
